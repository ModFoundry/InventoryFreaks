package dev.derpierre65.inventoryfreaks.neoforge;

import dev.derpierre65.inventoryfreaks.Inventoryfreaks;
import net.neoforged.fml.common.Mod;

@Mod(Inventoryfreaks.MOD_ID)
public final class InventoryfreaksNeoForge {
    public InventoryfreaksNeoForge() {
        // Run our common setup.
        Inventoryfreaks.init();
    }
}
